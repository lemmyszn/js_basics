// Declare and initialize the number variable
const number = 42;  // You can change this value to test different numbers

// Check if the number is positive, negative, or zero
if (number > 0) {
    console.log(`${number} is positive`);
} else if (number < 0) {
    console.log(`${number} is negative`);
} else {
    console.log(`${number} is zero`);
}

// Additional examples with different numbers
console.log("\nTesting with different numbers:");

// Test with a negative number
const negativeNumber = -15;
if (negativeNumber > 0) {
    console.log(`${negativeNumber} is positive`);
} else if (negativeNumber < 0) {
    console.log(`${negativeNumber} is negative`);
} else {
    console.log(`${negativeNumber} is zero`);
}

// Test with zero
const zeroNumber = 0;
if (zeroNumber > 0) {
    console.log(`${zeroNumber} is positive`);
} else if (zeroNumber < 0) {
    console.log(`${zeroNumber} is negative`);
} else {
    console.log(`${zeroNumber} is zero`);
} 